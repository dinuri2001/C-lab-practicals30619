#include <stdio.h>
int main(){
    int num,i,f;
    printf("Enter a number: \n");
    scanf("%d",&num);
    printf("The factors of %d are:",num);
    for(i=1;i<=num;i++)
    {
        if (num%i==0){
            printf("%d \n",i);
            
        }
      
        
    }
    
    
return 0;
}
