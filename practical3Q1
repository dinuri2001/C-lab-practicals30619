#include <stdio.h>
int main() {
    int num1,num2,max;
    printf("enter two numbers");
    scanf("%d %d",&num1,&num2);
    if (num1>num2)
    max=num1;
    else
    max=num2;
    printf(" the highest value is %d \n",max);
    
